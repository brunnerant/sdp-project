<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">
    <group android:checkableBehavior="single">
        <item android:id="@+id/home"
            android:icon="@drawable/ic_home"
            android:title="@string/menu_home"/>
        <item android:id="@+id/my_quizzes"
            android:icon="@drawable/ic_format_list_bulleted"
            android:title="@string/menu_quiz_list"/>
        <item android:id="@+id/online_quizzes"
            android:icon="@drawable/ic_search"
            android:title="@string/menu_search"/>
        <item android:id="@+id/qr_code_burger"
            android:icon="@drawable/ic_camera"
            android:title="@string/menu_qr"/>
        <item android:id="@+id/my_account"
            android:icon="@drawable/ic_person"
            android:title="@string/menu_person"/>
        <item android:id="@+id/settings"
            android:icon="@drawable/ic_settings"
            android:title="@string/menu_settings"/>

    </group>
</menu>